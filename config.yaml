# 3D BREP 模型分类部署配置
# ========================================

# 模型配置
model:
  # 模型路径（二选一）
  # 方式1: 使用导出的模型文件（推荐生产环境）
  model_path: "pt/model.pt"
  # 模型参数
  graph_emb_dim: 256
  
  # 推理设备
  device: "cuda"  # "cpu" 或 "cuda"
  
  # 批量推理大小
  batch_size: 8

# 类别映射配置
class_mapping:
  # 父类映射（英文 -> 中文）
  parent_classes:
    zhengti: "整体式"
    zhuzao: "铸造式"
    huanxing: "环形式"
  
  # 子类映射（英文 -> 中文）
  child_classes:
    che: "车削"
    li: "里"
    liwo: "螺窝"
    wo: "窝"
    wuzhou: "无轴"
  
  # 完整类别映射（路径 -> 类别ID）
  # 根据实际训练数据调整
  full_class_map:
    "huanxing": 0
    "zhuzao": 1
    "zhengti": 2
 

# UI配置
ui:
  title: "🔬 3D BREP 模型智能分类系统"
  description: "上传STEP格式的三维模型，自动提取BREP拓扑结构并进行分类预测"
  theme: "dark"
  
  # 功能开关
  enable_3d_preview: true
  enable_batch_processing: true
  max_batch_size: 20
  
  # 服务器配置
  server_port: 5001
  share: false  # 是否生成公网链接

# 临时文件目录
temp_dir: "/tmp/brep_deploy"
