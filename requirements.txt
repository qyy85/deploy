# 3D BREP 模型分类部署模块依赖
# ========================================

# ============================================
# Web UI框架
# ============================================
gradio>=6.0,<7.0  # Gradio 6.x 版本
huggingface_hub>=0.20,<0.26  # 兼容 Gradio 6.x
fastapi>=0.104.0  # FastAPI 用于静态文件挂载
uvicorn[standard]>=0.24.0  # ASGI 服务器
python-multipart>=0.0.6  # FastAPI 文件上传支持

# ============================================
# 核心依赖（复用项目现有依赖）
# ============================================
# torch>=2.0.0           # 已在主项目中安装
# dgl>=2.0.0             # 已在主项目中安装
# numpy>=1.24.0          # 已在主项目中安装

# ============================================
# 配置和工具
# ============================================
PyYAML>=6.0
tqdm>=4.65.0

# ============================================
# 可选：3D可视化增强
# ============================================
# trimesh>=4.0.0         # STEP文件解析（可选）
# pythreejs>=2.4.0       # Jupyter 3D预览（可选）

